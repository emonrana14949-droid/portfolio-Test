i love my mom i love my baba
import pandas as pd

df1 = pd.DataFrame({
    'key': ['a','b','c','d','e'],
    'A1': range(5),
    'B1': range(5,10)
})

df2 = pd.DataFrame({
    'key': ['a','b','c'],
    'A1': range(3),
    'B': range(3,6)
})

df1
df2
result = pd.merge(df1, df2, how='inner', on='key')

result[['key','A1_x','B1']]
pd.merge(df1,df2,how='left', on='key')
